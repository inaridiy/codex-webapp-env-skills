interface:
  display_name: "Modern Webapp Environment"
  short_description: "Bootstrap Codex-friendly webapp tooling"
  default_prompt: "Use $modern-webapp-environment to check git status, ask me which modules to install, then apply and verify the selected setup."
